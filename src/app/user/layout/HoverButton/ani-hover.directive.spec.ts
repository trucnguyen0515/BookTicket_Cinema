import { AniHoverDirective } from './ani-hover.directive';

describe('AniHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new AniHoverDirective();
    expect(directive).toBeTruthy();
  });
});
